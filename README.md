# RoomSync

Smart hostel & roommate management web app.

## Project structure
- `backend/` – Node.js + Express API
- `frontend/` – React (Vite) app

## Getting started (backend)
```bash
cd backend
cp .env.example .env
npm install
npm run dev
```
